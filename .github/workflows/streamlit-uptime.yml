name: Streamlit Uptime Ping

on:
  schedule:
    - cron: "*/20 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping deployed Streamlit app
        run: |
          curl --fail --silent --show-error --location \
            "https://ai-attendance-email-system-ngph2wz4ss7m27zer5frwt.streamlit.app/" \
            > /dev/null
